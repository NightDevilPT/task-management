import React from "react";
import { VerifyForm } from "@/components/shared/forms/verify-form";

const page = () => {
	return (
		<div className="w-full h-screen flex justify-center items-center">
			<VerifyForm />
		</div>
	);
};

export default page;
