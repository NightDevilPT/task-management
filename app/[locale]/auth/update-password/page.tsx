import React from "react";
import { UpdatePasswordForm } from "@/components/shared/forms/update-password-form";

const page = () => {
	return (
		<div className="w-full h-screen flex justify-center items-center">
			<UpdatePasswordForm />
		</div>
	);
};

export default page;
